// ! Booleans: true / false
let on = true;
console.log(on);

let off = false;
console.log(off);

//! Null
let empty = null;
console.log(empty);

//! undefined
let undef = undefined; // instead of assigning our undef variable a datatype of undefined, we can simply just declare the variable.
console.log(undef);

//! Numbers
let degrees = 90;
console.log(degrees);

let precise = 999999999999999; //15
console.log(precise);

let rounded = 9999999999999999; //16
console.log(rounded);

let notQuite = 0.2 + 0.1;
console.log(notQuite); //JS rounds out a certain number, so if math is needed, be aware.

let numbersArehard = (0.2 * 10 + 0.1 * 10) /10;
console.log(numbersArehard);

//! Strings
// represent text
// wrapped in single or double quotes

let stringOne = "double quote";
let stringTwo = 'single quotes'

console.log(stringOne, stringTwo);
let first = 12354 + 567;
let second = "1234" + "567"


console.log(first);
console.log(second);

//! Objects

let frodo = {
    race: "hobbit",
    rings: 1,
    string: true,
}

console.log(frodo);

//! Arrays
let list = ['item 1 ', 'item 2', 'item 3'];
      (1)  (2)            (3)

/*
1. name of the aray  - list
2. the array is with in square brackts
3 each item , regardless of data type is spearated by commas
*/
console.log(list);

//! DataType Literals

let car = "Ford ";
let dec = 12;
